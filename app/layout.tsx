// Root layout — wraps every page in the app. In the App Router, this file
// is required and controls the <html>/<body> shell.
//
// WHAT TO DO:
// 1. Export a default function `RootLayout({ children })` that returns
//    <html><body>{children}</body></html>
// 2. Export a `metadata` object (title, description) — Next.js reads this
//    automatically for the page <head>.
//
// LATER, ONCE AUTH EXISTS:
// Think about whether you want a global "auth state" available to every
// page (e.g. via React Context) so you don't re-check login status on
// every single page. Not required to start — build the login page and
// dashboard first, wire this up once you feel the duplication.
